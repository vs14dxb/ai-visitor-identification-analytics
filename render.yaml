services:
  - type: web
    name: ai-visitor-tracker
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host=0.0.0.0 --port=10000"
    envVars:
      - key: UVICORN_PORT
        value: 10000
    plan: free
    autoDeploy: true